# This is the server type
# Available types:
# general, other
server-type: general

# This section defines 'enabled' status for internal modules
# Default state is 'true'
# Some combinations can result in errors so be careful
# Modules: Auth AuthKeys Classrooms Discord Slides Timetable UStudy
disabled-modules:
  - "Timetable"