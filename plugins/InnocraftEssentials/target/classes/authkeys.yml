groups:
  participant:
    # This discord role will be given
    roles: ["705449535324946502"]
    groups: ["participant"]
    # These commands are executed right after the player redeems his auth-key
    # {player} will be changed to player's name